[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llmadapt"
version = "0.3.0"
description = "A lightweight, provider-agnostic LLM agent with tool-calling, context compression, rank-based routing, and hardware-aware local model support for Anthropic, OpenAI, Gemini, and Ollama."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "Shyam Parikh", email = "shyamparikh@yahoo.com" }
]
keywords = ["llm", "agent", "tool-calling", "anthropic", "openai", "gemini", "ollama", "context-compression", "local-llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Shyam-Parikh-2025/llmadapt"
Issues = "https://github.com/Shyam-Parikh-2025/llmadapt/issues"

[tool.setuptools.packages.find]
where = ["src"]
